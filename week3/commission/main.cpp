#include <iostream>

using namespace std;

int main()
{
    int amount, total=0, rate1=0.05,rate2=0.1,rate3=0.15,amount1=100000,amount2=300000;

    if(amount < amount1) {
        total = amount * rate1;
    }
    if(
    return 0;
}
